import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Vincular Cuenta de Epic Games',
  description: 'Vinculá tu cuenta de Epic Games para poder recibir cosméticos de Fortnite vía regalo.',
};

export default function VincularCuentaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
